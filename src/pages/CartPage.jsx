import { CartItemsList } from "../components/CartItemsList";

const CartPage = () => {
  return (
    <div>
      <h1>Your Cart</h1>
      <CartItemsList />
    </div>
  );
};

export default CartPage;
